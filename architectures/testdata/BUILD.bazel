package(default_visibility = ["//visibility:public"])

# Tokenizer / config fixtures only — model weights come from Hugging Face Hub.
filegroup(
    name = "maykeye_tinyllama_v0",
    srcs = glob(
        ["maykeye-tinyllama-v0/**"],
        exclude = ["maykeye-tinyllama-v0/**/*.safetensors"],
    ),
)
