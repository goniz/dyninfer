load("@rules_cc//cc:cc_test.bzl", "cc_test")
load("@rules_shell//shell:sh_test.bzl", "sh_test")

package(default_visibility = ["//visibility:private"])

cc_test(
    name = "paged_kv_runtime_api_compile_test",
    srcs = ["paged_kv_runtime_api_compile_test.c"],
    deps = ["//bazel/iree:runtime_cc"],
)

sh_test(
    name = "paged_kv_abi_test",
    srcs = ["paged_kv_abi_test.sh"],
    args = [
        "$(rootpath //bazel/iree:iree_compile_bin)",
        "$(rootpath :paged_kv_abi.mlir)",
    ],
    data = [
        ":paged_kv_abi.mlir",
        "//bazel/iree:iree_compile_bin",
    ],
)
